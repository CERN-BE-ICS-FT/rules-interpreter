syntax = "proto3";
package rules;

service RulesService {
    rpc RunRules (RulesRequest) returns (RulesReply);
}

message RulesRequest {
    string context = 1;
}

message RulesReply {
    string result = 1;
}