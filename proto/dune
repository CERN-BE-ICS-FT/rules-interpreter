(library
 (name rules_service)
 (libraries ocaml-protoc-plugin))

(rule
 (targets rules_service.ml)
 (deps
  (:proto rules_service.proto))
 (action
  (run
   protoc
   -I
   .
   "--ocaml_out=annot=[@@deriving show { with_path = false }]:."
   %{proto})))