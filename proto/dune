(library
 (name rules_grpc)
 (libraries ocaml-protoc-plugin))

(rule
 (targets rules_grpc.ml)
 (deps
  (:proto rules_grpc.proto))
 (action
  (run
   protoc
   -I
   .
   "--ocaml_out=annot=[@@deriving show { with_path = false }]:."
   %{proto})))