(library
 (name rules)
 (libraries ocaml-protoc-plugin))

(rule
 (targets rules.ml)
 (deps
  (:proto rules.proto))
 (action
  (run
   protoc
   -I
   .
   "--ocaml_out=annot=[@@deriving show { with_path = false }]:."
   %{proto})))